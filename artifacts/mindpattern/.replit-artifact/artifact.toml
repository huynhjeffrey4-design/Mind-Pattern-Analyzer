kind = "web"
previewPath = "/"
title = "MindPattern"
version = "1.0.0"
id = "artifacts/mindpattern"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25243

[services.development]
run = "pnpm --filter @workspace/mindpattern run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mindpattern", "run", "build" ]
publicDir = "artifacts/mindpattern/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25243"
BASE_PATH = "/"
